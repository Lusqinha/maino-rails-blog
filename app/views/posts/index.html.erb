<h1><%= t('posts.title') %></h1>

<% @posts.each do |post| %>
  <h2><%= link_to post.title, post %></h2>
<% end %>

<%= link_to t('posts.new_post'), new_post_path if user_signed_in? %>

<p><%= paginate @posts %></p>