<h1>Posts</h1>
<% @posts.each do |post| %>
    <h2>
        <%= link_to post.title, post %>
    </h2>
    <p>
        <%= paginate @posts %>
    </p>
    <% end %>
        <%= link_to "Novo Post" , new_post_path if user_signed_in? %>